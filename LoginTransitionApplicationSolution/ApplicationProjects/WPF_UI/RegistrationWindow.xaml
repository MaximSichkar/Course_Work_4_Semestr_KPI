<Window x:Class="WPFSystemAplication.RegistrationWindow"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFSystemAplication"        
        xmlns:ManagmentSystemViewModels="clr-namespace:Account.VM;assembly=Account.VM"
        mc:Ignorable="d"
        Title="Registration Window"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding WindowLoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Border Background="#FF2B2B2B" CornerRadius="10" Padding="10" BorderBrush="#FF444444" BorderThickness="2" Margin="150,10,150,11">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="115*"/>
                    <RowDefinition Height="88*"/>
                </Grid.RowDefinitions>
                <Label Content="Registration" HorizontalAlignment="Center" Foreground="White" FontSize="20" Margin="0,0,0,188"/>

                <Label  Foreground="White" FontSize="14" Margin="10,47,10,114">
                    <TextBlock Text="{Binding NotificationMessage}" TextWrapping="Wrap" Width="446" Height="58" />
                </Label>

                <Label Content="Email" Foreground="White" FontSize="14" Margin="142,125,272,73"/>
                <TextBox Margin="142,157,143,53" Text="{Binding AccountModel.Email}" Width="200" x:Name="TextBox"/>

                <Label Content="Password" Foreground="White" FontSize="14" Margin="142,190,262,11"/>
                <TextBox Text="{Binding AccountModel.Password}" Width="200" x:Name="PasswordBox" Margin="142,224,143,161" Grid.RowSpan="2"/>

                <Button Content="Register" Command="{Binding RegisterCommand}" Background="#FF5A5A5A" Foreground="White" FontSize="14" Margin="172,108,173,36" Grid.Row="1"/>                
            </Grid>
        </Border>
        <Button Content="Back to Login" Background="Transparent" BorderBrush="Transparent" Foreground="White" FontSize="14" Click="Button_Click_1" Margin="300,289,300,139"/>
    </Grid>
</Window>
