<Window x:Class="WPFSystemAplication.LoginWindow" 
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFSystemAplication"
        xmlns:ManagmentSystemViewModels="clr-namespace:Account.VM;assembly=Account.VM"
        mc:Ignorable="d"
        Title="Login Window"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding WindowLoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <!-- Custom Window Design -->
        <Border Background="#FF2B2B2B" CornerRadius="10" Padding="10" BorderBrush="#FF444444" BorderThickness="2" Margin="246,36,246,36">
            <Grid>

                <Label Content="Login" HorizontalAlignment="Center" Foreground="White" FontSize="20" Margin="0,0,0,307"/>

                <Label  Foreground="White" FontSize="14" Margin="10,47,10,246">
                    <TextBlock Text="{Binding NotificationMessage}" TextWrapping="Wrap" Width="254" Height="51" />
                </Label>

                <!-- Main Content -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0">                    

                    <Label Content="Email" Foreground="White" FontSize="14"/>
                    <TextBox Text="{Binding AccountModel.Email}" Width="200" x:Name="TextBox"/>

                    <Label Content="Password" Foreground="White" FontSize="14"/>
                    <TextBox Text="{Binding AccountModel.Password}" Width="200" x:Name="PasswordBox"/>

                    <Button Content="Login" Command="{Binding LoginCommand}" Width="200" Background="#FF5A5A5A" Foreground="White" FontSize="14" Margin="0,10,0,0"/>

                    <Button Content="No account? Register one!" Background="Transparent" BorderBrush="Transparent" Width="200" Foreground="White" FontSize="14" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
